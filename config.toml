[api]
api_key = ""                    # or set env var API_KEY
base_url = "https://apihub.agnes-ai.com/v1"
scoring_model = "agnes-2.0-flash"
summary_model = "agnes-2.0-flash"
price_in_per_m = 0
price_out_per_m = 0

[last30days]
enabled = true
engine_path = "~/.hermes/skills/last30days/scripts/last30days.py"
python_path = "python3.12"
timeout = 60
search_sources = "reddit,hackernews"

[pipeline]
lookback_days = 1
dedup_window_days = 90
content_cap = 4000
output_dir = "output"
history_path = "data/pushed.json"
fetch_timeout = 15
fetch_workers = 8
score_workers = 4
log_level = "INFO"

[insight]
sync_enabled = true
